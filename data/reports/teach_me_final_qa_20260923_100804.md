# TeachMe Final QA — 20260923_100804

Baseline: commit `0f68c44` (TeachMe UI hedef UX VERIFIED). Push yok. Bu koşuda yalnızca iki QA açığı kapatıldı.

## MULTISELECT UI

| Kontrol | Sonuç |
|--------|--------|
| Genel | **PASS** |
| 10 selected | PASS (`test_multiselect_context_teach_flow[10]`) |
| 20 selected | PASS (`test_multiselect_context_teach_flow[20]`) |
| 50 selected | PASS (`test_multiselect_context_teach_flow[50]`) |
| right-click / context menu populate | PASS (`_populate_multi_teach_menu` + sticky menu; `menu.exec` bypass testable) |
| common candidates | PASS (intersection via `_quick_pick_candidates` / `card.rivals`) |
| multi-candidate teach | PASS (≥2 checkbox → “Seçilenleri … olarak öğret” → `_apply_teach`) |
| successful removal | PASS (pool’dan düşüş + teach worker idle) |
| failure restore | PASS (`test_multiselect_teach_failure_restores`; optimistic snap geri) |
| &lt;10 no multi-teach menu | PASS (`test_multiselect_below_10_no_teach_menu`) |
| Crash / freeze / deleted QObject / Shiboken / QThread | PASS (suite temiz; worker `qthread_is_running` ile idle bekleniyor) |

Notlar:

- Fixture DB (`upsert_review` + tmp `patterns.db`); production veri dokunulmadı.
- Mevcut path: `card.rivals` → ortak aday → `TeachMeTeachWorker` / `teach_files` → optimistic remove/restore.
- Yeni öğrenme sistemi yok. Teach backend mimarisi değişmedi (bu commit’te `core/teach_me.py` dahil edilmedi).

## PREVIEW REGRESSION

| Alan | Değer |
|------|--------|
| Genel | **PASS** |
| ROOT CAUSE | Stale test beklentisi. `ImagePreviewDialog` ilk günden beri `lbl_image.setMinimumSize(320, 240)` + `apply_responsive_dialog` ile tercih edilen ~960×720 açılış. Test `minimumWidth() >= 640` istiyordu → `320 >= 640` FAIL. TeachMe UX patch yan etkisi değil; production minWidth yanlış değil. |
| BEFORE | `assert dlg.lbl_image.minimumWidth() >= 640` → FAIL |
| FIX | Production min değiştirilmedi (yalnızca açıklayıcı comment). Test: dialog `width/height` büyük (≥640/≥480); label floor ≥320/≥240 ve floor &lt;640. Assertion “PASS olsun diye” gevşetilmedi — doğru sözleşmeye hizalandı. |
| AFTER | `test_large_preview_opens_and_closes` PASS |

## REGRESSION

| Suite | Sonuç |
|-------|--------|
| `tests/test_teach_me.py` + async teach + status worker + multiselect | **43 passed** |
| `test_teach_me_preview` + `inbox_preview` + `card_layout` + `qthread_lifecycle` | **40 passed** |
| `test_large_preview` | PASS |
| Preview related (TeachMe) | PASS |
| QThread destroyed while running | **0** (lifecycle suite PASS) |
| StatusWorker already deleted | **0** (`test_status_worker_deleted_ref` PASS) |
| Teach backend mimarisi | Değiştirilmedi (scoped commit) |

## UI HEDEFİ (kapsam içi doğrulama)

Mevcut hedef UX (`0f68c44`) + yeni multiselect context testleri ile:

- Normal: adaylar gizli / Hover: kart adayları / Selected: checkbox / Multi: ortak adaylar / Right-click öğret / Success remove / Fail restore — regresyon kırığı yok.
- Global “Aday Kavramlar” paneli: YOK (değişiklik yok).

## DEĞİŞEN DOSYALAR (bu görev)

- `ui/teach_me_panel.py` — `_populate_multi_teach_menu` extract (testable; davranış aynı)
- `tests/test_teach_me_multiselect_context.py` — yeni Qt UI fixture testleri
- `tests/test_teach_me.py` — `test_large_preview` beklenti düzeltmesi
- `ui/preview_dialog.py` — yalnızca comment
- `data/reports/teach_me_final_qa_20260923_100804.md` — bu rapor

## ÖZET SKOR

| | |
|--|--|
| 10–50 multi-select/right-click | **PASS** |
| test_large_preview | **PASS** |
| TeachMe regression | **PASS** |
| FAIL | 0 |
| WARNING | 0 (yalnızca Qt deprecation uyarıları; davranışsal değil) |
| UNTESTED | 0 |
| BLOCKED | 0 |

PUSH: yapılmadı.
